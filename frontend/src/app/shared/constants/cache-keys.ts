/** Centralised sessionStorage keys to prevent silent cross-component drift. */

/** Timestamp of the last successful price-data cache write (ISO string). */
export const CACHE_TIMESTAMP_KEY = 'np-cache-timestamp';
